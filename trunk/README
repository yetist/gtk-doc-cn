说明：
先将tools/gtkdocpo 复制到$PATH所在的路径中。
然后进入zh_CN/下的任一目录。如zh_CN/libgksu
运行../../tools/gadt-html,便可在当前目录生成相应的发布文档。

以gdk-pixbuf为例，说明一下生成最终文档的方法：
sudo cp tools/gtkdocpo /usr/bin
cd zh_CN/gdk-pixbuf
../../tools/gadt-html

如果能成功编译gtk+, 那么将在zh_CN/gdk-pixbuf/html目录下生成最终的文档。

