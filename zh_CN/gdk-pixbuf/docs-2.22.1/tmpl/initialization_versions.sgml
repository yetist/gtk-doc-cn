<!-- ##### SECTION Title ##### -->
初始化和版本 (Initialization and Versions)

<!-- ##### SECTION Short_Description ##### -->

库的版本号。

<!-- ##### SECTION Long_Description ##### -->
<para>
可以使用这些宏和变量来检测您所链接的 &gdk-pixbuf; 的版本号。
</para>

<!-- ##### SECTION See_Also ##### -->
<para>

</para>

<!-- ##### SECTION Stability_Level ##### -->


<!-- ##### VARIABLE gdk_pixbuf_version ##### -->
<para>
包含 &gdk-pixbuf; 库的完整版本号的字符串。这个是运行的程序使用的当前版本号。
</para>


<!-- ##### VARIABLE gdk_pixbuf_major_version ##### -->
<para>
&gdk-pixbuf; 库的主版本号。 （例如 &gdk-pixbuf; 版本1.2.5 的主版本号为 1）
</para>

<para>
这是个库中的变量，所以表示的是您链接的 &gdk-pixbuf; 库的主版本号。对比 #GDK_PIXBUF_MAJOR 
宏，这个宏表示的是您的程序包含的 &gdk-pixbuf; 头文件的主版本号。
</para>


<!-- ##### VARIABLE gdk_pixbuf_minor_version ##### -->
<para>
&gdk-pixbuf; 库的次版本号。 （例如 &gdk-pixbuf; 版本1.2.5 的次版本号为 2）
</para>

<para>
这是个库中的变量，所以表示的是您链接的 &gdk-pixbuf; 库的次版本号。对
比 #GDK_PIXBUF_MINOR 宏，这个宏表示的是您的程序包含的 &gdk-pixbuf; 
头文件的次版本号。
</para>


<!-- ##### VARIABLE gdk_pixbuf_micro_version ##### -->
<para>
&gdk-pixbuf; 库的修订版本号。 （例如 &gdk-pixbuf; 版本1.2.5 的修订版本号为 5）
</para>

<para>
这是个库中的变量，所以表示的是您链接的 &gdk-pixbuf; 库的修订版本号。对
比 #GDK_PIXBUF_MICRO 宏，这个宏表示的是您的程序包含的 &gdk-pixbuf; 
头文件的修订版本号。
</para>


<!-- ##### MACRO GDK_PIXBUF_VERSION ##### -->
<para>
包含 &gdk-pixbuf; 头文件的完整版本号的字符串。与 #gdk_pixbuf_version 对比，
这个是正被编译的版本号。
</para>



<!-- ##### MACRO GDK_PIXBUF_MAJOR ##### -->
<para>
&gdk-pixbuf; 库的主版本号，比如版本"0.8.0"中的第一个"0"就是主版本号。
</para>



<!-- ##### MACRO GDK_PIXBUF_MINOR ##### -->
<para>
&gdk-pixbuf; 库的次版本号，比如版本"0.8.0"中的"8"就是次版本号。
</para>



<!-- ##### MACRO GDK_PIXBUF_MICRO ##### -->
<para>
&gdk-pixbuf; 库的修订版本号，比如版本"0.8.0"中的最后一个"0"就是修订版本号。
</para>



